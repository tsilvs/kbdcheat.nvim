local M = {}

M.options = {
	height = 4,
	content = {
		"Normal mode  <- Esc | ^C",
		"Insert mode  <- i | a | I | A | o | O",
		"Command mode <- : | / | ?",
		"Replace mode <- R",
		"Visual mode  <- v"
	}
}


return M
